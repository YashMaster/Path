#"restart" or "relaunch" or "reload" to close and re-open the console 
Write-Host "not implemented..."
#will do the following:
#	1. OG>  Get the cmdline args for the current proc
#	2. OG>  create a new (hidden) tmp process 
#	3. tmp> kill OG
#	4. tmp> start a new process and kill itself
#	5. new> kill tmp and OG 

